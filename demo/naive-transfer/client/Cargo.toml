[package]
name = "naive_transfer_client"
version = "0.1.0"
edition = "2021"

[dependencies]
borsh = { version = "1.5", features = ["derive"] }
clap = { version = "4.5.20", features = ["derive"] }

solana-client = "1.18"
solana-program = "1.18"
solana-sdk = "1.18"